apiVersion: v1
kind: Service
metadata:
  name: {{ include "clickcat.fullname" . }}
  labels:
    {{- include "clickcat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clickcat.selectorLabels" . | nindent 4 }}
