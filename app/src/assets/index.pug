- const favicon = require('assets/images/favicon.png');
doctype html
html
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(id="theme-color" name="theme-color" content="#1e1e1e")
    //- 
    title ClickCat
    //- 
    link(rel="shortcut icon" type="image/png" sizes="any" href="assets/logo.svg")
    
    //- Styles
    each file in htmlWebpackPlugin.files.css
      link(rel="stylesheet" href=file)
    script(id="initialStateScript").
      !{initialState}
    style 
      include index.css
    script.
      var _hmt = _hmt || [];
      (function() {
        var hm = document.createElement("script");
        hm.src = "https://hm.baidu.com/hm.js?747b1f7022aeaf1b9ad311f71610c6e8";
        var s = document.getElementsByTagName("script")[0]; 
        s.parentNode.insertBefore(hm, s);
      })();
     
  body
    #root.
      !{appHtml}
    //- Not need with SSR, but need to show when app data loading 
    #appLoader.
      !{appLoaderHtml}

    //- App JS
    //- each file in htmlWebpackPlugin.files.js
    //-   p #{file}

    each file in htmlWebpackPlugin.files.js
        script(defer src=file)
